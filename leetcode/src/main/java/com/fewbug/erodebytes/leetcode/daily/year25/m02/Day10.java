package com.fewbug.erodebytes.leetcode.daily.year25.m02;

/**
 * @author chunhang.xch
 * @Description
 * @date 2025/2/10 09:27
 **/
public class Day10 {

    /**
     * 3 <= graph.length <= 50
     * 1 <= graph[i].length < graph.length
     * 0 <= graph[i][j] < graph.length
     * graph[i][j] != i
     * graph[i] 互不相同
     * 猫和老鼠在游戏中总是可以移动
     *
     * @param graph
     * @return
     */
    public int catMouseGame(int[][] graph) {

        return -1;
    }
}
